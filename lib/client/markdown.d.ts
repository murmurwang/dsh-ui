import * as React from "react";
export interface MarkdownViewProps {
    source: string;
}
/** 预览用 Markdown 视图。 */
export declare function MarkdownView({ source }: MarkdownViewProps): React.ReactElement;
